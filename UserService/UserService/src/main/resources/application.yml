server:
  port: 8081

spring:
  application:
    name: USER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/ride_hailing_app_user_servicedb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
